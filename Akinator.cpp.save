#include "SayKat.h"

int main()
    {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    setlocale(LC_ALL,".1251");
    /*
    char* text = (char*) calloc(100, sizeof(char));

    text = "Вітаю тебе, друже мій милий, не бажаєш зіграти?";
    long long SizeOfBase = 0;
    while(*(text + SizeOfBase) != '\0')
        {
        ++SizeOfBase;
        }


    FILE* Base = fopen("base.txt", "wb");
    fwrite(&SizeOfBase, sizeof(long long), 1, Base);
    fwrite(text, sizeof(char), SizeOfBase, Base);

    */
    FILE* Base = fopen("base.txt", "rb");
    long long SizeOfBase = 0;
    fread(SizeOfBase, sizeof(long long), 1, Base);
    char* text = (char*) calloc(Siz, sizeof(char));

    return 0;
    }
